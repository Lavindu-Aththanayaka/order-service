PORT=8082
DB_HOST=localhost
DB_PORT=5432
DB_NAME=orderdb
DB_USER=order_user
DB_PASSWORD=changeme

# Base URL for the inventory-service this order-service depends on.
# In Kubernetes this becomes the inventory-service's cluster DNS name,
# e.g. http://inventory-service.ecommerce.svc.cluster.local
INVENTORY_SERVICE_URL=http://localhost:8081
